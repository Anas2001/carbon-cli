module.exports = (name) =>
    `(*               Associated library                *)
(***************************************************)
import IntUtils
library ${name}Library

contract ${name}Contract()
`;